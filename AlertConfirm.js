alert("Enter the value of a ")
let a = prompt("Enter the value of a")
a = Number.parseInt(a)
alert("You entered a of type")
ler write = confirm("Do u want write it to the page")
if(write)
{
  document.write(a)
}
